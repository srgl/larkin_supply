h1 Loads

.toolbar
  a.btn.btn-default href="#{new_load_path}" Create load
  a.btn.btn-default.delete-btn.disabled href="#{loads_path}" Delete load(s)

.grid
  table.table.table-striped
    thead
      tr
        th
          input#select-all-checkbox type="checkbox"
        th Load Number
        th Delivery Date
        th Delivery Shift
        th Total Volume

    tbody
      - @loads.each do |load|
        tr
          td
            input.select-checkbox type="checkbox" value="#{load.id}"
          td == link_to load.load_number, edit_load_path(load)
          td = load.delivery_date
          td = load.delivery_shift
          td = load.total_volume

